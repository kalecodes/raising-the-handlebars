<div>Welcome to your event dashboard</div>
<br>
<section class="post-list">
  {{#each events as |event|}}
  <div>
    {{> event-info event }}
  </div>
  {{/each}}
</section>
<aside>
  Hi, {{ activeUser }}, welcome to Handle.
  <br>
  Click <button>here</button> to add a new event.
  <br>
  <div>
    Select a guideline tag below to view related events:
    {{#each tags as |tag|}}
    <div>
    {{> tags-info tag }}
    </div>
    {{/each}}
  </div>
</aside>